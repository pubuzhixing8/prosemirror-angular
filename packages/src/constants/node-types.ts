export enum MarkTypes {
    bold = 'strong',
    italic = 'em',
    code = 'code',
    underline = 'underline',
    strike = 'strike',
    color = 'color',
    bgColor = 'bg-color'
}

export enum InlineTypes {
    link = 'link'
}

export enum BlockTypes {
    horizontal_rule = 'horizontal_rule',
    todo_item = 'todo_item'
}
